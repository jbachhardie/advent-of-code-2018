const R = require('ramda');

/**
 * @param input {string[]}
 * @returns {number}
 */
exports.standard = R.always(0);

/**
 * @param input {string[]}
 * @returns {number}
 */
exports.plus = R.always(0);
